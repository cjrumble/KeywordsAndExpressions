package academy.learnprogramming;

public class Main {

    public static void main(String[] args) {
	// int int2=5;
	// false true null
        // a mile is equal to 1.609344 kilometers
        double kilometers = (100 * 1.609344);
        //variables(kilometers), values(1.609344), and operators(=,*) = expression
        // keywords as-in Java's 53 Reserved Words
        int highScore = 50;
        if (highScore == 50) {
            System.out.println("This is an expression");
        }
// In the following code, write down which parts of the code are expressions.
        int score = 100;
        if (score > 99) {
            System.out.println("You got the high score!");
            score = 0;
        }
    }
}
